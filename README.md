Leaky File Upload.

steps to run:
change .env.example to .env
run npm install
run npm run start:dev
open localhost:3000

used technologies and why:
we used supabase as postgresql database to save images details.
we used typeorm as orm to interact with the database.
we used cloudinary to store images.
